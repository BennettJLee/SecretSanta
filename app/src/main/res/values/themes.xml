<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SecretSanta" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimaryDark">@color/primaryRed</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#f4e4a9</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/secret_santa_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.SecretSanta</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
    </style>
</resources>